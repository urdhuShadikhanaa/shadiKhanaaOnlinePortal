export class CategoryHelper {
    static HIERARCHY_LINK_CATEGORY = '__standard';

    static SEARCH_RESULT_CATEGORY_PREFIX = 'palette-';

    static SEARCH_RESULT_CATEGORY_PREFIX_REGEX = /palette-/;
}